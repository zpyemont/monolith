apiVersion: kubeflow.org/v1
kind: TFJob
metadata:
  name: fashion-two-tower-csv-batch
  namespace: default
spec:
  runPolicy:
    cleanPodPolicy: None
    backoffLimit: 5
    ttlSecondsAfterFinished: 86400
  tfReplicaSpecs:
    PS:
      replicas: 1
      restartPolicy: OnFailure
      template:
        metadata:
          labels:
            app: fashion-two-tower-csv
            role: ps
        spec:
          terminationGracePeriodSeconds: 30
          nodeSelector:
            cloud.google.com/gke-spot: "true"
          tolerations:
            - key: cloud.google.com/gke-spot
              operator: Equal
              value: "true"
              effect: NoSchedule
          serviceAccountName: ranker-sa
          containers:
          - name: tensorflow
            image: gcr.io/looksyuk/monolith-fashion-two-tower:latest
            imagePullPolicy: Always
            command: ["python"]
            args:
              - "/fashion_model_two_tower.py"
              - "--training_type=batch"
              - "--model_dir=/checkpoints/fashion_two_tower"
              - "--model_name=fashion_two_tower"
              # CSV training data from GCS
              - "--fashion_batch_csv=gs://looksy-monolith-data/training_data/synthetic_training_data_positive_only.csv"
              # Training parameters
              - "--max_steps=50000"
              - "--save_checkpoints_steps=5000"
              - "--batch_epochs=50"
            env:
              - name: ZK_SERVERS
                value: "monolith-zookeeper-client.default.svc.cluster.local:2181"
              - name: TF_CPP_MIN_LOG_LEVEL
                value: "1"
            resources:
              requests:
                cpu: "1"
                memory: "4Gi"
              limits:
                cpu: "2"
                memory: "8Gi"
            volumeMounts:
              - name: checkpoint-volume
                mountPath: /checkpoints
          volumes:
            - name: checkpoint-volume
              persistentVolumeClaim:
                claimName: monolith-checkpoint-pvc

    Worker:
      replicas: 1
      restartPolicy: OnFailure
      template:
        metadata:
          labels:
            app: fashion-two-tower-csv
            role: worker
        spec:
          terminationGracePeriodSeconds: 30
          nodeSelector:
            cloud.google.com/gke-spot: "true"
          tolerations:
            - key: cloud.google.com/gke-spot
              operator: Equal
              value: "true"
              effect: NoSchedule
          serviceAccountName: ranker-sa
          containers:
          - name: tensorflow
            image: gcr.io/looksyuk/monolith-fashion-two-tower:latest
            imagePullPolicy: Always
            command: ["python"]
            args:
              - "/fashion_model_two_tower.py"
              - "--training_type=batch"
              - "--model_dir=/checkpoints/fashion_two_tower"
              - "--model_name=fashion_two_tower"
              # CSV training data from GCS
              - "--fashion_batch_csv=gs://looksy-monolith-data/training_data/synthetic_training_data_positive_only.csv"
              # Training parameters
              - "--max_steps=50000"
              - "--save_checkpoints_steps=5000"
              - "--batch_epochs=50"
            env:
              - name: ZK_SERVERS
                value: "monolith-zookeeper-client.default.svc.cluster.local:2181"
              - name: TF_CPP_MIN_LOG_LEVEL
                value: "1"
            resources:
              requests:
                cpu: "2"
                memory: "8Gi"
              limits:
                cpu: "4"
                memory: "16Gi"
            volumeMounts:
              - name: checkpoint-volume
                mountPath: /checkpoints
          volumes:
            - name: checkpoint-volume
              persistentVolumeClaim:
                claimName: monolith-checkpoint-pvc
